name: 图片融合并压缩
steps:
  - id: blend
    uses: custom-image-blend-script
    with:
      mix_ratio: ${{ params.mix_ratio }}
  - id: compress
    uses: custom-1770146354922
    needs: [blend]
    with:
      quality_num: ${{ params.quality_num }}
